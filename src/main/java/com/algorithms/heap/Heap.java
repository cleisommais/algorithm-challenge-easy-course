package com.algorithms.heap;

public class Heap {
    public void maxHeap(int[] array, int index, int size) {
        int largest = index;
        while (largest < size / 2) {
            int left = (2 * index) + 1;
            int right = (2 * index) + 2;
        }
    }
}
